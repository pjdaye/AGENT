FROM aist.python3

USER root

RUN bash -c "pip3 install supervisor-stdout"

COPY . /app

COPY Deployment/agent/supervisord.conf /etc/supervisor/conf.d/supervisord.conf

ENV PATH /bin:/usr/bin:/usr/sbin:/usr/local/bin:/usr/local/sbin:/sbin

CMD /usr/bin/supervisord